## What this changes

<!-- One or two sentences. What is different after this merges. -->

## Why

<!-- The problem it solves. If it fixes an issue, write "Fixes #N". -->

## Checks

- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm test` passes
- [ ] `npm run build` completes — this site prerenders ~100 routes, so a
      broken page fails here and nowhere else
- [ ] Copy changes read in the site's voice: lowercase, concrete,
      no superlatives, no claim without a source

## Screenshots

<!-- For anything visual, before and after. Delete if not applicable. -->
